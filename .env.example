# Required for live analysis. Server-side only; never use NEXT_PUBLIC_ for secrets.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
# Optional; improves GitHub REST rate limits. Only public endpoints are used.
GITHUB_TOKEN=
